{{#if _d.hasExtensiveUI}}
<!-- more UI -->
<mj-table class="body-table body-table-lower body-table-lower-moreui body-table-last {{#if isDark}}is-dark{{else}}is-bright{{/if}}">
	{{> tr-extensive-ui
		isFirst=true
		isLast=true
		what="calendar"
		link="{{rhythm.edit}}"
		alt="Périodicité"
		title="Cliquez ici pour visualiser et éditer la périodicité d'envoi"
		label="{{rhythm.next}}&emsp;<strong>{{rhythm.editlabel}}</strong>"
	}}
</mj-table>
{{/if}}
